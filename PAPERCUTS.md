The flat ESLint migration initially scanned generated Wrangler bundles under `presence/.wrangler`; keep that directory globally ignored.
`gh pr create` returned `401` when 1Password-backed GitHub credentials were unavailable; use the prefilled browser flow instead.
